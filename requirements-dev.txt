# Development & CI tooling (not needed to run the app).
# Install with:  pip install -r requirements-dev.txt
#
# The app's runtime dependencies are in requirements.txt.

-r requirements.txt

pytest>=8.0
ruff==0.15.20
mypy==2.1.0
pyflakes>=3.2
# numpy 2.5 stubs use the Python 3.12 'type' statement, which mypy (python_version 3.10) rejects.
numpy<2.5
