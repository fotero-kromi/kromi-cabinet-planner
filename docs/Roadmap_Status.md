# Audit Roadmap - Status

Tracks the implementation of the principal audit (docs/Principal_Audit_v34.47.md). Every release passed the full ceremony: tests written to fail first, full suite, byte-identity gates on the standard flows (golden dumps and exported sheets), lint and type gates, allow-list packaging and a fresh-extract run.

## Done

| Release | Audit items | What changed |
|---|---|---|
| v34.48 | C2 (P0), C3, C6, C7, C10, S4, S10, F9 | Server bound to this computer; error details out of the browser; formula-safe Excel/CSV exports; failed AI batches no longer cached; unreadable override database reported (never silent); missing KROMI numbers and skipped sheets reported with the export; KTC-ID checked in every mode; atomic preference writes; allow-list release packager; isolated tests |
| v34.49 | C4, C5, pandas 3 | Working CI gate plus dependency audit; tests pass from any folder and on Streamlit 1.58 and 1.64; Streamlit floor 1.54 (CVE); run.bat keeps dependencies in sync; plain messages for unreadable uploads; overrides ready for pandas 3; README rewritten to match the code |
| v34.50 | C11, C12 | Auto-mapping never collides; header row in every mode with detection; customer codes stay exact text; override-set scope exact; "Update existing set" merges onto the chosen set; size fixes reset per file |
| v34.51 | UX F10-F15 | Confirmations before destructive technician actions; AI default follows the key; optimizer on demand with the real overfill factor; corrected texts; accessible button contrast |
| v34.52 | Owner request, C10 class | New mode "Fixed configuration (existing machines)": machines per supply point plus headroom; most used articles placed first; overflow may move to a type the article fits (switchable); articles without space marked "Not placed", never dropped; sidebar shows only the controls that apply; workbook adds Fixed_Configuration and Not_placed. Also: truthful supply-point captions with the Program mapping; the Article setup sheet is no longer dropped silently when its numbers overflow |
| v34.53 | C8 | Every run is archived (a later run with new codes used to fail with a KeyError); each run links to the classifications it used, so an AI run after a heuristic one is kept, the snapshot shows the right source and a recompute reads the run's own answers; a known workbook reuses the newest AI or manual answer; technician overrides are never stored as the classifier's answer |
| v34.54 | C9 | "Load & recompute" restores every input (PPE sheet, per-class thresholds, restockable categories, Program-to-supply-point map, site, KTC-ID, customer label), checks the mapping at the run's header row, keeps the optional mapping, preselects the override choice the run applied, and reports "Restored N of M settings"; runs record the applied override set; deleting a set keeps the runs; build, model and classifications are in the duplicate-save check |
| v34.55 | Reliability table (migrations, logging, save race, DB path) | Each schema migration is one transaction and an existing database is copied before migrating; a rotating local log records every error the app catches on purpose; two sessions saving the same new workbook no longer collide; a bare KROMI_DB_PATH file name works |
| v34.56 | Owner request (first real fixed-configuration test) | Optional "Current stock pcs" mapping (found automatically, never a min/max/safety level or a stock location); one "Takeover sheet SP n" per supply point in the takeover template's columns (Cust. Prop. Art. Nr., Kunden Art. Nr., Bezeichnung 1/2, VPE, im KTC, am HLO, Total, Schranktyp) plus a separate takeover download. Whole packs go into the KTC up to the maximum (Carousel: compartments x VPE; Helix: spirals x places per spiral by size S 28 / M 22 / L 18 / XL 12 x VPE), the rest stays at the HLO; lockers, Kanban and not-placed articles hold nothing in the KTC. Numbering fix: a provided "Stufenbohrer" structure word now gives code 14 (was 13); threading inserts stay 12 (structure family 806) |
| v34.57 | Field bug | "Treat as Helix-fit (M)" / "Select all and apply" did nothing on a workbook the database already knew: the stored-classification reuse (v34.53) ran after the manual size fix and put the old L/XL size back. The fix now runs after the reuse and wins |
| v34.58 | Owner request | "Stock-based Helix promotion" in the fixed configuration (off by default, needs the stock column, "Stock covers (months)" default 3): after the fit, the free Helix space (headroom kept) takes S/M Carousel articles whose stock / VPE / months exceeds the Helix threshold and the recorded use, highest first, each with the spirals the normal Helix sizing gives; freed Carousel space goes to articles without space; status "Promoted"; the takeover maximum follows the spirals |
| v34.59 | Professionalization step 0, repository | Synthetic golden gate: an invented, deterministic catalog drives the page through four scenarios (standard, capped with two replicated supply points, fixed configuration with Program mapping, stock and promotion, numbering only); plan tables and exported workbooks must match a committed manifest in every test run and in CI. `tools/check.py` is the one quality gate (lint, types, customer names stored only as hashes, wording); customer file types are git-ignored. The project moved to a private GitHub repository with `CLAUDE.md` (working rules) and `docs/`; development continues in Claude Code on the web |
| v34.60 | Hotfix, quality gate | The gate passes on fresh installs and on Windows: plan digests no longer depend on the platform line ending, every text file read names UTF-8 (checked by a test), numpy below 2.5 for the type check, a Windows CI job (reporting, not yet blocking) |
| v34.61 | Rewrite preparation | The tool-list building and the Summary, Run_Metadata, audit, bucket comparison and distribution tables moved from the Streamlit page into the engine (`engine/tool_list.py`, `engine/export_frames.py`); the setting defaults and choice labels live once in `engine/planning_defaults.py`, read by the page and later by the new app. Results and exports byte-identical |

Suite: 2,200 passed / 25 skipped / 0 failed (v34.61). The standard planning flows are byte-identical to v34.47 (since v34.54 the golden capture leaves out the duplicate-save hash, which now carries the build; since v34.56 the export capture pins the stock unmapped). The KDS onboarding file gives a cell-for-cell identical Article setup except its three step drills, which now carry code 14.

## Owner decisions

1. **C1 number registry: dropped (2026-09-22).** KROMI numbers are not stored between runs. They stay a per-run result the owner decides to share, because articles move between KTC and Kanban and the parameters change the numbers.
2. **Fit articles into existing machines: done in v34.52.** Demand follows the existing rules; the most used articles first; less used ones flagged when they do not fit; the headroom stays empty; in this mode the sidebar keeps only the relevant controls.
3. **Takeover (2026-09-22):** every takeover is customer property, one sheet per supply point; maximum = allocated compartments x packaging unit, filled up to the maximum; lockers ignored for now.
4. **Stock as a demand hint (2026-09-23):** where consumption is missing, the customer's stock is assumed to cover about 3 months; used only to move articles into free Helix space (switchable), never to change routing or other placements.
5. **Repository and tooling (2026-09-23):** the code lives in a private GitHub repository; development moves to Claude Code on the web. Release zips remain available through `tools/package_release.py`.
6. **Rewrite prototype (2026-09-23):** a branch `rewrite/fastapi-react` prototypes the app without Streamlit (`docs/Rewrite_Decision.md`). Confirmed: FastAPI + React/TypeScript, PostgreSQL for the new app (it starts with an empty database), Docker for the prototype; the engine is shared, no page logic is copied (hence v34.61). Streamlit stays on `main`, with SQLite, until parity.

## Next

- Rewrite prototype: the vertical slice on `rewrite/fastapi-react` (upload, mapping, standard run, results, workbook download) matching the synthetic `standard` scenario, in three PRs: back end and parity, front end, Docker and end-to-end tests.
- If the rewrite is stopped: Option A of `docs/Code_Professionalization_Plan.md` from step 1 (vocabulary enums, sidebar, intake, classification stage, results, ExportContext).
- Smaller open items: upload size guard and stored-workbook retention (S5), keep results visible (dimmed) after a setting change instead of hiding them (C13), autosave of unsaved technician corrections.
- Offered, not confirmed: "Placed (incl. moved)" label; a warning when a supply point with machines receives no articles.
